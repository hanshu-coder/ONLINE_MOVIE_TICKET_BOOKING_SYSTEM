spring.application.name=ticket
# Server Configuration
server.port=8081

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/ticketdb?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Priya@2005
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Logging
logging.level.org.springframework=INFO
logging.level.com.ticketbooking=DEBUG

# CORS (If using frontend on different port)
# You can manage CORS globally or use @CrossOrigin at controller level
